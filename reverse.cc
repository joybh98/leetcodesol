#include <iostream>
using namespace std;

int main() {
	int arr[] = {1,2,3,4,5};
	int d = 4;
	int i;
	int arr_size = sizeof(arr)/sizeof(arr[0]);

	for(int i =0;i<=arr_size;i++) {
		
	}

	cout << arr << endl;
	return 0;
}